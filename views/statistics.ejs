<% layout('layout') -%>

    <section>
        <div class="container">
            <div class="row">
                <% occuranciesByName.forEach((occurancy)=> { %>
                    <div class="col-md-12">
                        <h2>
                            <%= occurancy._id.name %> - <%= occurancy.totalCount %> pieces - <%= occurancy.sum %>€
                        </h2>
                    </div>
                    <% }); %>
            </div>
        </div>
    </section>